# Triage tools reuse the parent venv (../requirements.txt already provides
# requests, tqdm; GitPython is available but not required by these scripts).
# Install only if running outside the repo venv:
requests>=2.31
tqdm>=4.66
